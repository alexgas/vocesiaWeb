import * as React from 'react';

const Template = ({ className = ''}) => {
    return (
        <div>
            <h1 className={className}>template</h1>
        </div>

    );
}

export default Template;
